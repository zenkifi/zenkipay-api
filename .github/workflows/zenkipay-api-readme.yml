# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2022-11-17T02:26:44.324Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Readme update

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - 'main'
    paths:
      - 'Zenkipay-API-v1-EN.yaml'
      - 'Zenkipay-API-v1-ES.yaml'
      - '.github/workflows/zenkipay-api-readme.yml'

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@8.0.4
        with:
          rdme: openapi Zenkipay-API-v1-ES.yaml --key=${{ secrets.README_API_KEY_ES }} --id=6385114fdba94b001faaf816

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@8.0.4
        with:
          rdme: openapi Zenkipay-API-v1-EN.yaml --key=${{ secrets.README_API_KEY_EN }} --id=638511ce05e6a1001e20dd8a
